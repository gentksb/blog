---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';

const posts = (await getCollection('post')).sort(
	(a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
---

<!DOCTYPE html>
<html lang="ja" data-theme="corporate">
	<head>
		<BaseHead title="幻想サイクル" description="テスト中" />
		</head>
	<body>
		<Header />
		<main>
			<div class="container">
			<section>
				<div class="grid grid-cols-2 md:grid-cols-3 gap-4">
					{
						posts.map((post) => (
								<div class="card card-compact">
								<a href={`/post/${post.slug}/`}>
									<div class="absolute badge badge-accent rounded-none">
										{post.data.tags[0]}
									</div>
									<div class="aspect-[16/9] max-w-full overflow-hidden">
										<img src={post.data.cover.src} alt="カバー画像" class="object-cover" />
									</div>
									<div class="card-body">
									<h4 class="title">{post.data.title}</h4>
									<p class="date">
										<FormattedDate date={post.data.date} />
									</p>
								</a>
								</div>
								</div>
						))
					}
				</div>
			</section>
			</div>
		</main>
		<Footer />
	</body>
</html>
