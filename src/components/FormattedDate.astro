---
import { formatInTimeZone } from "date-fns-tz"

interface Props {
  date: Date
}

const { date } = Astro.props
---

<time datetime={date.toISOString()}>
  {formatInTimeZone(date, "Asia/Tokyo", "yyyy/M/d")}
</time>
